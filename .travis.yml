language: objective-c

before_script:
  - gem install cocoapods
  - brew update
  - brew upgrade xctool
script:
  - xctool -workspace ProxyKit.xcworkspace -scheme ProxyKitTestsiOS -sdk iphonesimulator7.1 ONLY_ACTIVE_ARCH=NO test
  - xctool -workspace ProxyKit.xcworkspace -scheme ProxyKitTestsMac -sdk macosx10.9 ONLY_ACTIVE_ARCH=NO test